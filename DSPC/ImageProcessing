#include <iostream>
#include <vector>

using namespace cv;
using namespace std;

// To convert an image to graph to be used by MST

struct Pixel {
    int x;
    int y;
    int intensity;
}

struct Edge {
    Pixel p1;
    Pixel p2;
    int weight;
}

vector<Pixel> convertMatToPixel(Mat image) {
}